// Centralized user-facing Scenario 1/2 labels.
// Change these values here; UI buttons and progress titles read from this source.
window.FIND_PAX_SCENARIO_COPY = Object.freeze({
  passenger: Object.freeze({
    joining: "Joining Passenger",
    transit: "Transit Passenger"
  })
});
