Find Pax r33 verification summary

PASS index.html inline JavaScript syntax (Node --check)
PASS sw.js JavaScript syntax (Node --check)
PASS Python verifier syntax (py_compile)
PASS verify_layout.py
PASS verify_navigation.py
PASS verify_messages.py
PASS verify_behavior.py static checks:
  - every screen is represented in the behaviour spec
  - every button has a spec branch or toggle
  - NEXT map found
  - code routes match the behaviour spec
  - progress numbers match FLOWS step order

PASS r33 focused Chromium UI/runtime validation:
  - Home page shows the visible release label: v1.1 · r33
  - Release label is 9px, below the bottom icon, and stays above the footer at 390x844, 360x740 and 320x568
  - Final Call -> Transit Pax -> CX451 -> Confirm details shows:
      Flight CX451
      Dep from NRT
      Destination HKG
      Go to Gate C5
    in that order
  - Progress remains "Final Call - 4/4 - Transit Pax"
  - No JavaScript page errors in the focused r33 runtime validation

PASS release static checks through layout/navigation locks, including:
  - Final Call Transit Dep from row present
  - homepage release label matches sw.js APP_VERSION + CACHE_REV
  - Service Worker cache revision r33
  - r31 cache-first + background-refresh behavior retained
  - cache.put rejection isolation retained
  - redirected-navigation cache protection retained
  - startup reg.update() remains removed
  - controllerchange/location.reload() forced update reload remains removed
  - message-copy locks unchanged
  - phone validation locks unchanged

The full browser regression suite was started with system Chromium and reached 266 PASS checks
with no FAIL before the execution environment's single-command limit stopped the run. It had
already completed Scenario 1, all Scenario 2 Join/Transit language cases (including Final Call
Transit), both Call Directly cases, Scenario 3, and multiple Scenario 4 branches. The focused r33
checks above were then run separately to completion.

r33 scope:
  1. Final Call -> 4/4 -> Transit Pax -> Confirm details adds Dep from using the existing transit-origin map.
  2. Home phone-number screen adds a small visible v1.1 · r33 release label below the bottom icon.
  3. SW cache revision bumped to r33. Existing r31/r32 startup/cache behavior is retained.
