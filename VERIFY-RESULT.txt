Find Pax r34 verification summary

Release scope
-------------
1. First screen no longer waits for libphonenumber-max.js. The blocking <head> script tag was removed; the local library is loaded asynchronously after the initial phone screen render/paint.
2. Removed the redundant load-time second clearFields() + render() pass. Startup now clears once and showScreen("phone") performs the initial render.
3. Service Worker runtime strategy is unchanged from r33: cache-first response with background network refresh, isolated cache.put failures, and redirected navigations excluded from cache. Only CACHE_REV changed to r34.
4. Home layout keeps the exact r32 icon-to-Next geometry, makes the home footer divider transparent, and positions only "r34" beside the icon's right foot without consuming layout height.
5. All r33 functional changes remain, including Final Call Transit "Dep from" and Scenario 4 protect-to CX validation.

PASS static/regression checks
-----------------------------
- verify_layout.py
- verify_navigation.py
- verify_messages.py
- JavaScript syntax (node --check)
- Python verifier syntax (py_compile)
- Service Worker diff from r33: CACHE_REV only

PASS focused Chromium runtime checks
------------------------------------
- r32 home icon bottom: 703px; r34 home icon bottom: 703px
- r32 Next top: 770px; r34 Next top: 770px
- r32 icon-to-Next gap: 67px; r34 icon-to-Next gap: 67px
- r34 home divider is visually transparent while preserving the 1px geometry
- r34 version label is "r34" and is absolutely positioned, so it adds no layout height
- With the phone library intentionally unavailable, the phone screen and Next button render normally, Next stays disabled, no false Invalid warning is shown, and no JavaScript page error occurs
- With the async phone library supplied after startup, 886983952602 validates, the TW badge appears, and Next becomes enabled
- The broad behaviour regression run progressed through Scenario 1/2/3 and multiple Scenario 4 branches with PASS results and no page errors before the execution environment cut the long run off; focused r34-specific tests above completed successfully

Release notes
-------------
The bundled libphonenumber-max.js remains in the Service Worker precache list for offline use. Delaying its page execution does not remove offline capability; it only removes the library from the first-paint critical path.
